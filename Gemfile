source "https://rubygems.org"

# Ruby 2.6 compatibility (local)
gem "jekyll", "~> 4.2"

# Some native dependencies (e.g., jekyll-watch) pull ffi.
# Pin ffi for Ruby 2.6 compatibility.
gem "ffi", "~> 1.15"

group :jekyll_plugins do
  gem "jekyll-seo-tag", "~> 2.8"
end
